PIT_DB_PATH=./data/pit.sqlite
SESSION_SECRET=change-me-in-local-dev-only
OPERATOR_EMAIL=pitboss@pit.local
# Payments: sandbox (default) | partner | partner_stub | live (live still refuses until PartnerWalletClient exists)
# PIT_PAYMENTS=sandbox
